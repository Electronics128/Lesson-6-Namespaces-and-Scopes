# Lesson 6: Namespaces and Scopes

# TODO 1: Global scope:
# - Create a global variable `g_var = "I am global"`.
# - Write a function that prints `g_var`.

# TODO 2: Local scope:
# - Write a function that defines a local variable `l_var = "I am local"` and prints it.

# TODO 3: Enclosing scope:
# - Write a function `outer()` that defines `e_var = "I am enclosing"`.
# - Inside it, define `inner()` that prints `e_var`.
# - Call `inner()` from `outer()`.

# TODO 4: The `global` keyword:
# - Modify the global variable `g_var` inside a function using the `global` keyword.

# TODO 5: The `nonlocal` keyword:
# - Modify the enclosing variable `e_var` in `outer()` using the `nonlocal` keyword.

# TODO 6: Variable shadowing:
# - Create a global variable `name = "Global"`.
# - Define a function with a local variable `name = "Local"` and print it.
# - Print the global `name` from outside the function.

# TODO 7: Built-in scope:
# - Import the `math` module and use `math.sqrt()` to calculate the square root of 16.
# - Print the result.

# TODO 8: Debugging scopes:
# - Write a function with scope-related issues.
# - Use print statements to debug and fix the problem.

# TODO 9: Nested namespaces:
# - Write a function `outer()` that defines a local variable and calls another function `helper()` to print it.

# TODO 10: Scope resolution order:
# - Demonstrate LEGB (Local, Enclosing, Global, Built-in) by writing a function that accesses all these scopes in order.
